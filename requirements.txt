# Gepinnt: neuere Versionen entfernen st.components.v1.html (Deprecation
# "removed after 2026-06-01"), das die App an vielen Stellen nutzt. 1.58.0 hat
# die Funktion noch (nur deprecated) + alle genutzten neuen APIs (st.fragment,
# width="stretch", st.columns vertical_alignment). Vor einem Upgrade erst die
# components.html-Aufrufe auf st.html/st.iframe migrieren.
streamlit==1.58.0
pandas
pydeck
fitparse
SQLAlchemy
extra-streamlit-components
qrcode[pil]

# Postgres-Treiber – nur fürs Cloud-/Server-Deployment (Linux) nötig,
# wenn DATABASE_URL auf eine Postgres-DB zeigt (z.B. Neon/Supabase).
psycopg2-binary; platform_system != "Windows"

# MTP-Zugriff auf Garmin-Uhren ohne Laufwerksbuchstaben – nur Windows/lokal.
# Auf einem Linux-Server nicht verfügbar (und dort auch nicht sinnvoll).
pywin32; platform_system == "Windows"
